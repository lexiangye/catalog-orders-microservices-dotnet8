# OrderService.ClientHttp (Placeholder)

Questo progetto è attualmente vuoto perché non ci sono altri microservizi che consumano le API di **OrderService** in modo sincrono.

In un'architettura scalabile, questo modulo ospiterebbe l'interfaccia `IOrderServiceClient` e la sua implementazione basata su `HttpClient`, facilitando l'integrazione per futuri consumatori.
