# catalog-orders-microservices-dotnet8
Progettino semplice con architettura a microservizi in .NET 8 (Catalogo + Ordini) con MySQL (EF Core migrations), chiamate HTTP resilienti (retry/timeout/circuit breaker) e saga event-driven su Kafka. Avvio locale con Docker Compose.
